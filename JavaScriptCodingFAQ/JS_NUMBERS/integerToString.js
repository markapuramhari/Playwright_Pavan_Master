function integerToString(number) {
    return number.toString();
}


const inputNumber = 12345;
const result = integerToString(inputNumber);

console.log(`The integer ${inputNumber} as a string is: "${result}"`);